# GENERATORS

## Infinity Geometry

```text
point → relationship → loop → recursion → emergence → new relationship → ∞
```

The generator has no required terminal generation.

## Main life generator

```text
CODE → LIFE → REPRODUCTION → ECOSYSTEM → ∞
```

Each stage produces the conditions for the next while preserving lineage.

## 60-second breathing kernel

```text
period = 60 s
seed = 8
fibers = 18
phase_offset = π
rungs = golden-ratio progression
```

Palette names retained in the generator vocabulary:

```text
amethyst
obsidian
ruby
emerald
sapphire
citrine
topaz
gold
```

Structure:

```text
breath(t) = sin(2πt/60)
strand_A(t) = sin(2πt/60)
strand_B(t) = sin(2πt/60 + π)
```

The dual strands remain phase-opposed while sharing the same period.

## Resource flow

```text
source → use → return → transform → source
```

Rule:

```text
WHAT FEEDS, RETURNS.
```

## Five-zone loop

```text
VISION → MEMORY → CORE → FLOW → CREATE → VISION
```

The path can close internally, extend outward, and return.

## Lattice body generator

Material-state vocabulary:

```text
crystalline
plasma
metal
organic
liquid
gas
void
pure light
```

Core requirements:

```text
portable lattice-core
reversible state transition
preserve identity through transformation
```

## Generator rule

A generator must be able to expose its seed, parameters, transformation, and output so a second run can be compared with the first.
